---
users:
  - name: admin
    username: "${ADMIN_USERNAME}"
    password: "${ADMIN_PASSWORD}"
    email: "${ADMIN_USERNAME}@${DOMAIN}.com"
    ssh_key_path: "${ADMIN_SSH_KEY}"
  - name: ansible
    email: "ansible@${DOMAIN}.com"
    ssh_key_path: "${ANSIBLE_SSH_KEY}"